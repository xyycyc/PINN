# 交付后重构记录

## 版本与边界

- 工作目录：`E:\code\ai_model`。
- 交付备份：[submitted](https://github.com/xyycyc/PINN/tree/submitted)，提交 `626dc244cc704f9b8b943903c20d1402a2605c83`。
- 备份基于原 `master` 提交 `53d402ac4355a40f554d960c217450c47f2f47c9`，另收录 268 个原本未跟踪且未被忽略的交付文档、截图、辅助脚本。
- 原始数据、权重、运行结果等继续遵守 `.gitignore`，不属于 GitHub 备份；使用交付版本仍需要这些本地资源。
- 重构分支：`codex/refactor-portability`；`submitted` 保留为交付快照。
- 基线检查：迁移后原有 62 项测试通过，12 条 PyTorch 旧 checkpoint 加载方式警告。

## 本轮实施范围

以现有代码为功能依据更新使用说明，保留命令名称、CLI 参数默认值、训练算法、数据划分、模型格式和产物格式；GUI 的 R1 默认值变化按用户批准实施。

1. 分离 CLI 参数声明与命令执行，按需加载训练依赖，减少查看帮助和导入包的启动开销。
2. 统一配置、CLI 与 GUI 的项目路径解析，支持包目录前缀与用户目录；不根据盘符猜测或改写外部数据路径。
3. 修复任务启动/结束的并发状态、停止任务时阻塞界面、后台线程调用 Tk 的问题。
4. 配置使用原子写入，保存全部表单前先完整收集，失败时保留原配置；重新加载遵守当前配置文件路径。
5. 滚轮只作用于指针所在表单，避免全局绑定互相覆盖。

## 行为调整与审批状态

R1、R5、R6 已获用户批准并实施。R2、R3、R4、R7 仍为待批准方案，维持现有行为。

| 编号 | 已确认的现状 | 建议与影响 |
| --- | --- | --- |
| R1（已批准并实施） | 训练、预测、校验、增量训练默认自动寻找最新数据清单；多个数据集共存时可能切换目标。 | 默认关闭“自动使用最新清单”，首次使用明确选择数据集；用户仍可手动开启自动发现。代价是首次运行多一步选择，已有明确配置继续保留。 |
| R2 | GUI 保存个人表单会直接改写版本控制中的 `window/settings.json`。 | 保留它作为交付默认模板，个人设置写入被 Git 忽略的 `window/settings.local.json`；无个人配置时读旧文件，显式指定配置文件的 API 不变。避免个人绝对路径混入后续提交。 |
| R3 | 批量实验和结果浏览已有代码，但主窗口只挂载八个主流程页。 | 恢复“结果分析”分组中的结果浏览页，批量实验先继续隐藏。便于训练后直接查看产物；会改变主窗口分组和现有界面兼容快照。 |
| R4 | `load_rule_rows()` 在打开/刷新模型列表时自动扫描 checkpoint、补登记并可能修复 CSV；浏览界面可能写磁盘。 | 日常浏览改为只读；通过明确的「同步本地模型」按钮触发扫描、补登记和格式修复。外部新增模型需要主动同步一次。待批准，当前保持原行为。 |
| R5（已批准并实施） | 数字框留空后保存为 `null`，下次启动可能崩溃。 | 保存时标出空数字并要求补全；读取历史坏配置时逐字段恢复内置默认值，保留有效字段，在日志明确提示；原文件保存前不变。 |
| R6（已批准并实施） | GUI 强制要求验证集，CLI 允许无验证集的单清单训练。 | 新增默认关闭的「允许无验证集训练」开关；缺少验证集时需明确勾选，无验证早停。已有验证集和集合训练规则不变，CLI 保持兼容。 |
| R7（待批准） | 平滑窗口比波形更长时，卷积可能增大输出长度，导致模型尺寸不匹配。 | 建议预处理时拦截并提示缩小窗口；另一个选项是自动缩小窗口。尚未收到处理方式确认，本轮不改变平滑行为。 |

## 后续需要单独定范围的事项

- 增量训练当前优化全部模型参数，没有冻结 CNN/LSTM 的用户开关。加入冻结策略属于训练功能变化，需要单独确定冻结哪些层、何时解冻以及验收数据。
- 固定节点 `residual_pinn` 已实现空间平滑与离散拉普拉斯损失；它并未完整实现带时间导数、热源和显式边界条件的瞬态热方程。升级物理模型需要明确任务定义和数据支持。
- 历史五测点误差、加速比是交付记录，不是本次重构重跑的结果。算法或输入变化后需要重新评估。

## 验证方法

从项目上级目录执行 `python -m pytest ai_model/tests -q`。结构拆分需要比对六个子命令的参数、默认值与帮助；路径、配置失败恢复和子进程生命周期增加针对性回归检查。启动耗时只比较同一台机器、同一解释器的帮助/导入操作，不作为训练速度结论。


## 结构与操作改进

- `cli.py` 只解析和分发；`cli_arguments.py` 定义参数；`commands.py` 将原来的长分支拆为六个处理函数。已逐个比对处理函数的 AST，业务执行语句与交付版一致。
- 包公共导出按需加载，旧导入名称继续可用。帮助命令在没有 site-packages 的 Python (`-S`) 下也能执行。
- `paths.py` 统一根目录、包名前缀和用户目录解析；没有批量替换外部绝对路径。
- `run.py` 支持在项目根或任意工作目录使用绝对脚本路径启动；`--gui` 打开界面，其余参数传给 CLI。
- 同机单次测量：`python -m ai_model --help` 从约 2.58 s 降至 0.08 s；`import ai_model` 从约 2.60 s 降至 0.04 s。该数字仅说明启动依赖减少，不能据此推断训练或推理加速。
- 自动化测试可以直接在仓库根运行 `python -m pytest -q`。


## 第一轮验证（2026-09-12）

- 完整测试：`python -m pytest -q -ra --disable-warnings`，78 passed、1 skipped、11 warnings，约 12.75 s。
- 跳过原因为本机 Anaconda/Tcl 反复初始化时出现 `invalid command name "tcl_findLibrary"`。对应的 `test_new_gui_controls_reach_cli_arguments` 在独立进程中复测通过；较早一轮出现的另一个 Tcl 初始化跳过项也已独立复测通过。没有删除或放宽这两项测试。
- 新增针对性测试覆盖轻量导入、两种启动入口、路径一致性、手动清单默认值、显式自动选择的兼容、配置写入失败恢复、配置对象隔离、进程结束竞态、异步停止、特殊字符命令、Tk 线程边界、过期状态事件、表单即时生效和滚轮作用范围。
- 完整隐藏窗口测试通过：八个主流程页面装配，自定义配置文件重载、日志保留，无 Tk 回调错误。
- 从外部临时工作目录通过 `run.py validate` 实际完成命令分发和数据需求核查，使用临时清单与临时产物目录。
- 六个子命令的参数/默认值/帮助快照完全一致；六个处理函数的业务执行语句 AST 与 `submitted` 一致。
- 新拆分模块静态错误检查与 `git diff --check` 通过。
- 未重训生产模型，未重新测量交付精度或推理加速比。旧 PyTorch checkpoint 加载警告仍保留。

## 本机迁移修复

仓库所有者仍为迁移前的 Administrators，导致普通 Git 命令报 dubious ownership。已在本机 Git 配置中仅添加 `E:/code/ai_model` 为可信目录，普通 `git status` 已验证可用。Python 解释器仍位于现有的 `D:/anaconda` 安装；本轮没有迁移或重装 Python 环境。

## 第二轮：界面与使用体验

根据用户的“检查优化、美观化界面并模拟测试”要求实施：

- 主题独立为 `window/theme.py`，统一字体、白色分组、浅色背景和操作按钮；修复全局字体覆盖标题样式的问题。
- 长说明移到输入框下方并随窗口换行；滚动页支持自动显示获得键盘焦点的输入框。
- 执行按钮使用功能名称；新增不启动任务的命令预览，状态区显示经过时间和运行结果。
- 日志默认收起，运行时展开，可继续手动切换；保留全部日志，不做截断。
- 路径浏览、原始数据扫描和 CLI 的源目录解析统一到 `resolve_source_path()`；保留历史 `database/...`，支持用户目录。
- 非法数字与非有限值在字段旁反馈并自动定位；保存全部表单失败保持原文件。
- 清理页增加滚动与固定操作栏；运行期间禁止删除，任务结束后使旧清理计划失效。
- 重载保留选中页、日志显示方式和滚动偏好；规则 CSV 复用已读取的文本，避免重复读取文件，自动登记行为仍待 R4 批准后调整。

本机原生窗口自动化工具两次启动失败（沙箱初始化错误）。因此使用 Tk 自身事件循环模拟操作，并通过 PrintWindow 捕获脚本创建的窗口；未将测试描述为人工或操作系统鼠标验收。

第二轮验证（2026-09-12）：

- 较早完整批次为 `84 passed, 11 warnings`；最终日志布局修复后的完整批次为 `82 passed, 2 skipped, 11 warnings`。
- 两项跳过仍因本机 Anaconda/Tcl 反复初始化时偶发读取失败，安装文件实际存在。`test_new_gui_controls_reach_cli_arguments` 和 `test_mousewheel_is_scoped_and_bindings_are_cleaned_up` 均在独立进程补测通过，没有修改测试的跳过条件。
- Tk 使用模拟通过 9 类场景、24 组页面与尺寸组合（`1000x640`、`1280x820`、`1440x900`），无 Tk 回调错误。除了控件边界，还实际检查各尺寸下展开日志后的文本高度；修复了首次展开时 Tk 后续布局覆盖分隔条位置的问题。
- 实际校验子进程完成，停止和错误状态恢复通过。截图和运行报告保存在本机 `audit/gui_usability/smoke/`。
- 静态错误检查与 `git diff --check` 通过。警告来自既有 PyTorch checkpoint 加载；没有重训生产模型或重测交付精度。


## 第三轮：全局审核与文档同步

按用户要求审核主流程、GUI、配置、清理、数据输入、依赖与现行文档，并继续推送到已授权分支。R5、R6 按明确批准实施；其余明确缺陷修复及验证证据见[本轮审核记录](audit_2026-09-12.md)。

本轮处理了损坏配置恢复、无验证集训练的显式选择、手动校验空路径、完整增量依赖清理、路径范围与同名保护、模型相对引用、非有限输入和错误清单结构。缺失的本机 Fortran 库已从仓库源码重新构建，并增加可复用的环境自检。

主 README、窗口说明、模型迁移、数据 schema、兼容基线、Fortran 说明与依赖注释已同步。新增文档索引和交付资料归档说明；历史 Word/PDF、研究和验收指标继续保留原始事实记录。
