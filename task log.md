# 单波形温度场降采样与预测改造 Task Log

## 0. 使用说明

- 本文件是本次改造的唯一任务主线。开始一项前将 `[ ]` 改为 `[-]`，完成并通过验收后改为 `[x]`。
- 不允许仅因代码已写完就勾选完成；必须满足该阶段的“完成条件”。
- 每次完成任务后，在“执行记录”中填写日期、结果、产物路径和遗留问题。
- 如果实验否定某条路线，保留任务和数据，在记录中写明“不采用”及依据，不删除历史。
- 当前状态：`[ ]` 未开始，`[-]` 进行中，`[x]` 已完成，`[!]` 阻塞。

## 1. 项目目标与硬约束

### 1.1 目标

- 将一个实验 case 表示为“单条接收波形 + 固定物理位置上的温度标签”。
- 将原始约 23 万个非结构化有限元节点确定性降采样到不超过 10,000 个代表节点。
- 首先建立直接预测降采样温度场的基线，再用严格重建误差判断是否采用 POD。
- 最终预测输出保留开尔文温度、物理坐标、节点/材料/界面身份和可追溯元数据。

### 1.2 不可违反的硬约束

- [x] `ai_model.window` 的窗口布局、Tab、按钮、字段、默认值、操作步骤和用户可见功能完全不变。
- [x] 现有 GUI 调用的 CLI 命令及参数保持兼容；不得要求用户增加新的手工操作。
- [x] `build-db / train / predict / validate / online-update / demo` 的既有入口保持可用。
- [x] 不把 `receiver_signal_rigid_motion.csv` 当成独立实验波形。
- [x] 不再用全零占位场代表真实实验温度场。
- [x] 温度落盘和最终导出使用 K；模型内部允许标准化，但必须可逆并记录统计量。
- [x] 同一批数据的训练集、验证集和测试集按 case 划分，禁止同一 case 泄漏到不同集合。
- [x] 接触界面上相同 `(x, y)` 的两侧节点不得因坐标相同而被静默合并。
- [x] 所有新增缓存/模型都携带版本信息；旧 checkpoint 行为不得被静默改变。

## 2. 已确认的数据事实

- 样例 case：`worker_01/case_0000_T0300p000K`。
- 热力节点 CSV：234,322 行、63 列；温度列为 `T`，坐标列为 `x, y`。
- 样例坐标范围：`x=[0, 0.1]`、`y=[0, 0.02]`。
- 存在 1,001 个重复 `(x, y)` 坐标，来源于接触界面重复节点。
- 主波形和刚体校正波形均为 65,751 个时间步；校正波形是同一观测的派生版本。
- 超声配置通过 `config.io.temperature_csv_path` 指向同一 case 的热力节点 CSV。
- 当前实验 CSV 导入路径生成 `(1, 64)` 全零 field，随后扩展成 `(24, 24)`；这不是实际温度标签，必须替换。

## 3. 阶段 A：基线冻结与兼容契约

### A1. 冻结 GUI/CLI 行为

- [x] 列出 `ai_model.window` 当前所有可见 Tab、控件、默认值和生成的 CLI 参数。
- [x] 为每个 GUI Tab 建立命令拼装快照或等价自动化测试。
- [x] 记录现有 CLI 子命令的 `--help` 输出及关键默认值。
- [x] 记录现有 `build-db → train → predict → validate` 最小流程的产物目录和文件格式。

完成条件：改造前兼容基线可自动重复；后续每个阶段都能用同一套回归测试比较。

### A2. 定义内部数据契约

- [x] 定义 case 唯一键：至少包含 worker、case ID 和温度标识。
- [x] 定义波形选择策略：默认使用原始或刚体校正版，并记录选择来源。
- [x] 定义温度场样本结构和 schema 版本。
- [x] 定义旧 manifest 的兼容读取规则。

建议的温度场结构：

```text
temperature_k:  float32 [P]
coordinates_m: float32 [P, 2]
node_ids:       int64   [P]
material_ids:   int32   [P]
interface_side: int32   [P]
sample_weights: float32 [P]
```

完成条件：schema 有验证函数；缺字段、点数不一致、单位不明确时明确报错。

## 4. 阶段 B：固定约 10,000 节点降采样

### B1. 网格一致性审计

- [x] 扫描全部可用 case，比较节点数、`node_id`、坐标、材料 ID 和界面标识。
- [x] 确认所有 case 是否共用完全相同的有限元网格。
- [x] 生成网格审计报告：一致 case 数、不一致 case、坐标容差和异常原因。

决策门：

- 网格一致：选定一次固定 `node_id` 子集，所有 case 直接索引。
- 网格不一致：建立固定参考坐标/参考网格，并为每个 case 保存确定性映射；不得按 CSV 行号硬对齐。

完成条件：任意两个合格 case 的第 `i` 个降采样标签具有相同物理含义。

### B2. 设计降采样算法

- [x] 设定目标点数 `P <= 10000`，默认建议 `P=10000`。
- [x] 识别外边界、材料界面、界面两侧重复节点和材料内部节点。
- [x] 强制保留关键边界及界面节点；其余点采用空间覆盖采样，而非 CSV 等间隔抽行。
- [x] 在各材料区域内按面积/节点分布分层分配采样预算。
- [x] 为采样点计算训练损失权重，避免密集区域支配误差。
- [x] 固定随机种子和算法版本，保证重复运行得到完全相同的节点集合。

完成条件：产生一个版本化的采样索引文件，并包含：

```text
sampling_version
source_mesh_fingerprint
node_ids
coordinates_m
material_ids
interface_side
sample_weights
sampling_method
sampling_seed
```

### B3. 降采样质量检查

- [ ] 绘制全网格与采样节点空间分布图。
- [ ] 单独绘制边界、材料界面及重复坐标两侧节点。
- [ ] 检查各材料区域的覆盖率和最近邻距离分布。
- [ ] 对具有非均匀温度的 case，比较全场与采样场的温度分布、极值和梯度区域覆盖。
- [x] 验证同一采样索引可以无歧义应用到所有合格 case。

完成条件：不存在材料区域、关键边界或界面被漏采；界面两侧身份可区分。

## 5. 阶段 C：真实实验数据建库

### C1. case 配对

- [x] 按 case 目录和超声 JSON 的 `temperature_csv_path` 配对波形与热力场。
- [x] 验证引用的 CSV 存在、case ID 一致、运行状态有效。
- [x] 明确原始波形与刚体校正波形的选择规则；派生文件不得重复计为不同 case。
- [x] 将配对失败原因写入审计报告，不静默跳过。

### C2. 保存真实标签

- [x] 用固定采样索引从每个热力 CSV 提取 `T`，形成 `temperature_k[P]`。
- [x] 替换现有实验数据的全零 `(1,64)` field 占位逻辑。
- [x] 波形仍按现有系统要求重采样和预处理，原始路径与长度写入 metadata。
- [x] 保存可逆的温度标准化统计量；禁止按单个 case 独立归一化到 `[0,1]`。
- [x] manifest 记录采样版本、网格指纹、温度单位、坐标单位和波形版本。

完成条件：随机抽查 case 时，缓存标签可逐点追溯回原始 CSV 的 `node_id/x/y/T`。

### C3. 数据集划分

- [x] 按 case 划分训练、验证和测试集。
- [x] 检查温度范围、材料、边界条件在各集合中的分布。
- [x] 保存划分清单和随机种子。
- [x] 防止原始波形和其刚体校正版跨集合形成信息泄漏。

完成条件：自动化泄漏检查通过。

## 6. 阶段 D：直接预测 10,000 点基线

### D1. 模型实现

- [x] 保留当前波形预处理和用户可见训练参数的行为。
- [x] 使用波形编码器生成共享潜变量。
- [x] 增加直接温度场输出头，输出 `P` 个标准化温度。
- [x] 必要时采用分块输出头，避免单个巨大全连接层占用过多显存。
- [x] 将输出反标准化为 K，并附带固定坐标和节点身份。

### D2. 损失与指标

- [x] 使用采样权重计算主要温度损失。
- [x] 报告全场 MAE、RMSE、最大绝对误差，单位全部为 K。
- [x] 单独报告边界、界面、各材料区域和高温区域误差。
- [x] 报告参数量、显存、训练时间和单 case 推理时间。

### D3. 基线验收

- [ ] 与“预测全局均温并铺满所有点”的朴素基线比较。
- [ ] 与当前旧模型可计算的公共指标比较，但不得把零占位场结果作为有效精度。
- [-] 保存固定测试集预测、误差图和指标 JSON/CSV。

完成条件：直接预测模型显著优于均温铺满基线，并形成后续 POD 比较基准。

## 7. 阶段 E：POD 可行性评估（先评估，后接模型）

### E1. POD 离线重建实验

- [ ] 只使用训练集拟合空间平均场和 POD 基；验证/测试集不得参与拟合。
- [ ] 使用采样权重或等价的加权内积，避免非均匀节点密度扭曲模态。
- [ ] 分别测试 `K=8,16,32,64,128,256,512`，可按结果扩展。
- [ ] 保存每个 K 的累计解释方差。
- [ ] 在验证集和测试集上计算纯 POD 投影的 K 单位重建误差。
- [ ] 单独计算边界、界面、材料区域和高温区域重建误差。

重要说明：本阶段只测量 POD 表示本身的误差上限，不训练波形网络。

### E2. POD 采用门槛

- [ ] 与项目方确认可接受的 MAE、RMSE、最大误差和界面误差阈值。
- [ ] 选择满足工程误差阈值的最小 K，而不是只看解释方差。
- [ ] 如果没有合理 K 能满足阈值，记录“不采用 POD”，继续使用直接预测基线或进入坐标解码路线。

完成条件：形成明确决策记录，包含所选 K、所有误差指标和采用/不采用原因。

### E3. 波形到 POD 系数模型（仅在 E2 通过后）

- [ ] 复用与直接预测基线相同的波形编码器设置。
- [ ] 输出 K 个 POD 系数，并重建 P 个温度点。
- [ ] 同时记录系数损失和重建后 K 单位温度损失。
- [ ] 在完全相同的数据划分上与直接预测模型比较。
- [ ] 比较精度、稳定性、参数量、训练速度和推理速度。

完成条件：只有在精度满足要求且综合表现优于或接近直接预测时，POD 才成为默认内部模型。

## 8. 阶段 F：坐标条件解码器（可选升级）

- [ ] 仅当直接预测/POD 无法满足需求或需要可变输出点数时启动本阶段。
- [ ] 输入至少包括波形潜变量、归一化坐标、材料 ID 和界面侧身份。
- [ ] 对相同 `(x,y)` 的界面两侧节点验证模型能输出不同温度。
- [ ] 训练时随机抽取坐标点，但评估时必须覆盖完整固定采样集。
- [ ] 与直接预测和 POD 使用相同测试集及 K 单位指标比较。

完成条件：证明坐标模型带来可量化收益后才能替换默认路线。

## 9. 阶段 G：预测产物与现有窗口适配

### G1. 输出适配层

- [x] 在模型后端与 `ai_model.window`/CLI 之间增加适配层，不修改用户操作流程。
- [x] 预测产物至少保存 `node_id,x_m,y_m,temperature_k`。
- [x] 如现有绘图必须使用规则矩阵，仅在展示层插值；原始预测节点表必须保留。
- [x] 展示插值不得覆盖或冒充模型原始输出。
- [x] 输出 metadata 记录 checkpoint、schema、采样版本、网格指纹和温度标准化参数。

### G2. 兼容性回归

- [x] 阶段 A 的全部 GUI 命令快照无非预期变化。
- [ ] 所有现有 Tab、按钮、日志、停止任务和结果浏览功能通过测试。
- [x] 现有 CLI 参数继续有效。
- [x] `build-db / train / predict / validate / online-update / demo` 完整冒烟测试通过。
- [x] Windows 中文路径、空格路径和 UTF-8 日志通过测试。
- [x] 旧 checkpoint 要么兼容加载，要么给出明确版本错误；禁止错误静默运行。

完成条件：用户不改变任何既有操作即可完成建库、训练和预测，并得到真实降采样温度场。

## 10. 阶段 H：最终验收

- [x] 数据追溯：任意预测位置能追溯到固定采样定义及物理坐标。
- [x] 单位正确：最终温度为 K，坐标为 m。
- [x] 形状正确：每个合格 case 恰有 `P` 个温度标签，默认 `P<=10000`。
- [x] 无泄漏：case 级划分检查通过。
- [ ] 精度达标：测试集全部工程指标满足确认后的阈值。
- [-] 界面可靠：重复坐标两侧不被合并，界面误差单独达标。
- [x] 可重复：固定种子下采样索引、数据划分和关键评估结果可复现。
- [x] 兼容通过：`ai_model.window` 用户可见功能与改造前完全一致。
- [x] 文档完成：README/数据 schema/模型版本/迁移说明已更新。

## 11. 必须保留的实验对照

| 实验 | 输出表示 | 目的 | 状态 |
| --- | --- | --- | --- |
| E0 | 全局均温铺满 P 点 | 最低朴素基线 | [ ] |
| E1 | 直接预测 P 点 | 主要基线 | [ ] |
| E2 | POD 纯投影重建 | 测量 POD 表示误差下限 | [ ] |
| E3 | 波形预测 POD 系数 | 判断 POD 端到端价值 | [ ] |
| E4 | 坐标条件解码 | 可选升级 | [ ] |

所有实验必须使用相同 case 划分，并报告相同的 K 单位指标。

## 12. 待确认的工程阈值

- [ ] 目标采样点数：默认 10,000，候选 2,500 / 5,000 / 10,000。
- [ ] 全场 MAE 上限：`待确认` K。
- [ ] 全场 RMSE 上限：`待确认` K。
- [ ] 最大绝对误差上限：`待确认` K。
- [ ] 界面区域 MAE 上限：`待确认` K。
- [ ] 单 case 最大推理时间：`待确认`。
- [ ] 最大允许显存/内存：`待确认`。

未确认这些阈值前，可以完成数据管线和实验，但不能宣称最终模型“达标”。

## 13. 执行记录

| 日期 | 任务 | 状态 | 结果/产物 | 遗留问题 |
| --- | --- | --- | --- | --- |
| 2026-07-12 | 创建任务日志 | 已完成 | 本文件 | 工程误差阈值待确认 |
| 2026-07-12 | A1-A2 兼容基线与 schema | 已完成 | `docs/`、`tests/`、`data_process/temperature_field.py` | 无 |
| 2026-07-12 | B1-B2 网格审计与固定采样 | 已完成 | `database/case_temperature_field/mesh_audit.json`、`sampling_index.npz` | B3 质量图与分布报告待完成 |
| 2026-07-12 | C 真实 case 建库 | 进行中 | `database/case_temperature_field/manifest.json`、三份划分 manifest | 旧 build-db 接口尚未切换；集合分布报告待完成 |
| 2026-07-12 | D/E 算法骨架 | 进行中 | `model/point_field.py`、`model/pod.py` | 尚未训练；工程阈值未确认 |
| 2026-07-12 | 已完成项缺陷修复与 Window 适配 | 已完成 | schema-v1 Dataset、sampling v2、checkpoint v2、节点预测表、10 项回归测试 | 正式精度训练、POD 与工程阈值仍待完成 |

## 14. 当前下一步

从阶段 A 开始，不直接修改模型：

1. 冻结 GUI/CLI 兼容基线。
2. 扫描所有 case，确认网格是否一致。
3. 根据审计结果实现固定节点降采样。
4. 在真实温度标签建库完成后，才开始直接预测和 POD 实验。
